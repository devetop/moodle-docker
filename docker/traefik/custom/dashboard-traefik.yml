http:
  routers:
    api:
      rule: "Host(`traefik.yourdomain.com`)" # Ganti dengan domain Anda
      entryPoints:
        - "websecure"
      service: "api@internal"
      middlewares:
        - "simpleAuth"
      tls:
        certResolver: "letsencrypt"
